# Copy this file to `staging.env` (which must stay gitignored) and fill in the values.
# `query-bioactivity-api.sh` reads it automatically.
#
# Get these via the guide → "Getting access & credentials", or ask the maintainer.
API_URL=
API_KEY=
